# --- DATABASE CONFIG (FOR DOCKER COMPOSE) ---
# Use 'db' as the hostname if using the provided docker-compose.yml
DATABASE_URL=mysql://mnvg_user:your_secure_password@db:3306/mnvg_gold

# --- SERVER CONFIG ---
PORT=3000
JWT_SECRET=your_ultra_secure_secret_key_here

# --- DB SECRETS (Matches Docker Compose) ---
ROOT_PASSWORD=your_root_password
DB_PASSWORD=your_secure_password

# --- SECURITY ---
CORS_ORIGIN=http://localhost:3000
